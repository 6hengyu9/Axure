# 仓库管理系统（WMS）原型 · Axure 搭建说明书

> 适用场景：**通用制造 / 贸易企业自营仓库**，SKU + 批次 + 库位 三级管理
> Axure 版本：**Axure RP 9 / 10 / 11 通用**（用到的功能都是 9 以来的稳定特性）
> 配套物：`prototype/` 目录下的 HTML 可点击原型 —— 是这份文档的**视觉参照答案**，
> 搭 Axure 时对着看，省掉「文字描述→界面长什么样」的翻译成本。

---

## 0. 这份文档怎么用

按第 14 章的顺序做，**不要按章节顺序做**。文档是「查阅结构」，第 14 章是「施工顺序」。

三类信息分工：

| 你想知道 | 看这里 |
|---|---|
| 页面长什么样 | `prototype/` 里点开看；辅以第 12 章线框 |
| 每个字段叫什么、什么类型 | 第 7 章 中继器数据集 |
| 点了会发生什么 | 第 9 章 交互用例表 |
| 状态怎么流转、什么时候拦截 | 第 8 章 状态机与业务规则 |
| 颜色字号圆角 | 第 11 章 样式规范 |

---

## 1. 交付物清单

```
wms-prototype/
├── Axure搭建说明书.md          ← 本文档
├── README.md                   ← 两份交付物的使用说明
└── prototype/                  ← HTML 可点击原型（视觉参照）
    ├── login.html              ← 登录页，双击这个开始
    ├── index.html              ← 主框架（含 17 个功能页面）
    └── assets/
        ├── style.css           ← 对应第 11 章「样式规范」
        ├── data.js             ← 对应第 7 章「中继器数据集」
        └── app.js              ← 对应第 9 章「交互用例表」
```

搭完 Axure 后，你的 `.rp` 工程应该包含：**2 个母版、18 个页面、1 个自定义元件库、22 个中继器**。

---

## 2. 系统范围与角色

### 2.1 业务闭环

```
基础数据（物料 / 库位 / 往来单位）
      │
      ├──→ 入库：新建 → 收货登记 → 质检 → 上架 → 完成
      │                                    │
      │                                    ↓
      │                            ┌──────────────┐
      │                            │  库存（现存量 │
      ├──→ 库内：移库 / 调拨 / 盘点 ←→  锁定量     │
      │                            │  可用量）    │
      │                            └──────────────┘
      │                                    │
      └──→ 出库：新建 → 审核 → 拣货 → 复核 → 发运 → 完成
                        │
                        └→ 每一次库存变动都写一条「库存流水」（只增不改）
```

**一条铁律**：库存不允许被任何页面直接改写，只能由单据过账驱动。盘点差异也要生成「盘盈入库 / 盘亏出库」流水。这条规则决定了原型里所有「保存」按钮的提示文案。

### 2.2 角色与可见范围

| 角色 | 编码 | 核心权限 | 数据范围 |
|---|---|---|---|
| 系统管理员 | R01 | 全部菜单 + 用户/参数配置 | 全部仓库 |
| 仓库主管 | R02 | 单据审核、盘点审核、报表 | 本人分管仓库 |
| 收货员 | R03 | 收货登记、质检、上架 | 指定仓库 |
| 拣货员 | R04 | 拣货、复核、移库执行 | 指定仓库 |
| 制单员 | R05 | 新建入/出库单（无审核权） | 指定仓库 |
| 查询员 | R06 | 只读：库存、流水、报表 | 全部仓库 |

> 原型演示用 **仓库主管（李娜）** 视角，菜单全开。评审时如果客户问「收货员看到的是什么」，
> 用第 9 章 `UC-9.1` 的角色切换交互演示：切角色 → 左侧导航按 `DB.permTree` 重新过滤。

---

## 3. Axure 工程结构（页面树）

直接照抄到 Axure 的「页面」面板。缩进即层级。

```
WMS 仓库管理系统
├── 00 登录                          login
├── 01 工作台
│    └── 仓库总览                    dashboard
├── 02 基础数据
│    ├── 物料管理                    base-material
│    ├── 仓库与库位                  base-location
│    └── 往来单位                    base-partner
├── 03 入库管理
│    ├── 入库单管理                  in-receipt
│    └── 收货与上架作业台            in-putaway
├── 04 出库管理
│    ├── 出库单管理                  out-list
│    └── 拣货作业                    out-pick
├── 05 库存管理
│    ├── 库存查询                    stk-query
│    └── 库存流水                    stk-ledger
├── 06 库内作业
│    ├── 移库与调拨                  wip-transfer
│    └── 库存盘点                    wip-stocktake
├── 07 报表分析
│    ├── 出入库汇总                  rpt-summary
│    └── 库龄与呆滞料分析            rpt-age
└── 08 系统管理
     ├── 用户管理                    sys-user
     ├── 角色与权限                  sys-role
     └── 操作日志                    sys-log
```

共 **18 个页面**（含登录）。详情页**不单独建页面**，全部用抽屉动态面板承载（见第 10 章），
理由：详情页做成独立页面会导致返回列表时筛选条件丢失，评审时客户一定会吐槽这一点。

---

## 4. 全局设置

Axure 菜单 →「项目」→ 相应设置项。

| 项 | 值 | 说明 |
|---|---|---|
| 画布宽度 | **1440 px** | 企业内网台式机主流分辨率；内容区 1224 px |
| 页面对齐 | 居左 | 不要居中，否则 1920 屏上左导航会飘到中间 |
| 草图效果 | 关闭 | 高保真原型，不要手绘风 |
| 全局字体 | `Microsoft YaHei`（备选 `PingFang SC`） | 见第 11.5 |
| 栅格 | 显示，间距 **8 px**，对齐开启 | 所有间距是 8 的倍数，唯一例外是 4px 的微调 |
| 自适应视图 | **仅建 1 个「桌面 1440」** | 不做响应式。WMS 是内网桌面系统，做移动视图纯属浪费工期 |

### 4.1 页面级默认

每个功能页面统一结构，**从上到下 4 段**：

```
┌────────────────────────────────────────────┐
│ 母版：顶栏（高 56）                         │
├──────────┬─────────────────────────────────┤
│ 母版：    │  ① 面包屑 + 页标题 + 右上角操作按钮 │  ← 页头，高 ~72
│ 左导航    │  ② 统计卡行（可选，高 88）          │
│ （宽 216）│  ③ 查询条件卡                      │
│          │  ④ 数据表格卡（中继器 + 分页）       │
└──────────┴─────────────────────────────────┘
```

内容区左右内边距 **20 px**，卡片之间垂直间距 **14 px**。

---

## 5. 母版（Masters）

只做 2 个。母版拆太细会让「拖进页面后要摆位置」的成本超过复用收益。

### 5.1 `M-顶栏` — 尺寸 1440 × 56

| 元件 | 位置 | 内容 | 交互 |
|---|---|---|---|
| 品牌块（矩形） | x0 y0，216×56 | 深色底 `#16202E` + Logo 方块 + 「智云仓 WMS」 | 无 |
| 折叠按钮 | x232 | `☰` | 见 `UC-5.1` |
| 仓库切换器 | x272 | `▩ 全部仓库 ▼` | 见 `UC-5.2` |
| 帮助按钮 | 右起第 3 | `?` | 打开「原型说明」抽屉 |
| 消息铃铛 | 右起第 2 | `🔔` + 红点 | 打开消息抽屉 |
| 用户区 | 最右 | 头像 + 姓名 + 角色 | 打开用户菜单 |

**关键**：品牌块要放在顶栏母版里，不要放在导航母版里。否则左上角那 216×56 的深色区会跟着导航一起折叠，顶栏出现缺口。

### 5.2 `M-左导航` — 尺寸 216 × 900（高度设为「拉伸」）

结构 = **8 个分组，每组一个动态面板**：

```
分组行（高 40）  [图标 18] [名称] [箭头 ▶]     ← 点击 = 展开/收起
  └ 子项（高 34，左内边距 44）                 ← 点击 = 跳页面
```

8 个分组和子项照抄第 3 章页面树。

三个状态要做出来，客户一眼就看这个：

| 状态 | 样式 |
|---|---|
| 默认 | 文字 `#8F9CAD`，底透明 |
| 悬停 | 文字 `#FFFFFF`，底 `#1E2A3A` |
| **选中** | 文字 `#FFFFFF`，底 `rgba(42,120,214,.16)`，**左侧 2px 蓝色竖条** |

Axure 做法：子项用「矩形 + 文本」，在「交互样式」里设 MouseOver；选中态用**选项组（Selection Group）**
命名 `nav-group`，保证同时只有一个高亮。左侧竖条用一个 2×34 的矩形，跟着选中态一起显示/隐藏。

---

## 6. 元件库（自定义 Widget Library）

新建 `WMS.rplib`，把这些做成元件拖着用。**这一步省不掉** —— 17 个页面里按钮出现 200 次以上，不做元件库后期改一次配色要点 200 下。

| 元件名 | 尺寸 | 说明 |
|---|---|---|
| `btn/主按钮` | 自适应 × 30 | 蓝底白字，圆角 6 |
| `btn/次按钮` | 自适应 × 30 | 白底 + `#D3D8E2` 描边 |
| `btn/危险按钮` | 自适应 × 30 | 白底 + 红字 `#D03B3B` |
| `btn/文字按钮` | 自适应 × 20 | 蓝字无边框，表格「操作」列专用 |
| `btn/小按钮` | 自适应 × 26 | 卡片右上角专用 |
| `field/输入框` | 150 × 30 | 含 hover / focus 交互样式 |
| `field/下拉框` | 150 × 30 | 右侧 10×6 三角 |
| `field/日期框` | 150 × 30 | 右侧日历图标 |
| `field/多行文本` | 自适应 × 62 | |
| `tag/状态标签` | 自适应 × 21 | **8 个配色变体**，见第 11.3 |
| `tile/统计卡` | 288 × 88 | 标签 + 26px 数值 + 环比 |
| `card/卡片` | 自适应 | 白底 + 1px `#E5E8EE` 描边 + 圆角 10 + 轻投影 |
| `card/卡片头` | 自适应 × 47 | 14px 加粗标题 + 灰色副标题 + 右侧操作区 |
| `steps/步骤条` | 自适应 × 30 | 圆点 22px + 连接线，三态：已完成 / 当前 / 未开始 |
| `tabs/选项卡` | 自适应 × 41 | 选中项蓝字 + 2px 下边框 |
| `meter/进度条` | 70 × 6 | 圆角 3，三档配色（见 11.3） |
| `pager/分页器` | 自适应 × 45 | 「共 N 条 / 每页 / 页码」 |
| `empty/空状态` | 自适应 × 140 | 大号淡图标 + 「没有符合条件的数据」 |
| `legend/图例` | 自适应 × 20 | 色块 14×3 + 序列名 |

---

## 7. 中继器数据集（字段字典）

**这是本文档最该被认真读的一章。** 每张表 = Axure 里一个中继器（Repeater）的数据集。
列名请**逐字照抄**，因为 `prototype/assets/data.js` 用的就是这套列名，两边对得上你才能拿 HTML 当参照。

### 7.1 `rep-material` 物料

| 列名 | 中文 | 类型 | 示例 |
|---|---|---|---|
| code | 物料编码 | 文本 | `M-1001` |
| name | 物料名称 | 文本 | 冷轧钢板 SPCC |
| spec | 规格型号 | 文本 | `1.2×1250×C` |
| type | 物料分类 | 枚举 | 原材料 / 半成品 / 成品 / 辅料 / 包装物 |
| cat | 细类 | 文本 | 金属板材 |
| unit | 基本单位 | 枚举 | 个 / 台 / 吨 / 米 / 桶 / 箱 / kg |
| safeMin | 安全库存下限 | 数值 | 20 |
| safeMax | 安全库存上限 | 数值 | 120 |
| onHand | 现存量 | 数值 | 86.5 |
| locked | 锁定量 | 数值 | 12 |
| price | 参考单价（元） | 数值 | 4280 |
| batchCtl | 批次管控 | 是 / 否 | 是 |
| status | 状态 | 启用 / 停用 | 启用 |

演示数据准备 **12 行**，务必包含这 3 个「坑位行」，评审时用来讲规则：

- `M-1002 外六角螺栓`：现存 3200 < 下限 5000 → 触发库存预警
- `M-1006 不锈钢圆管`：`status=停用` 且现存量 0 → 讲「停用不等于删除」
- `M-1001 冷轧钢板`：单位「吨」，数量带小数 → 讲「小数位按单位走，个/台不显示小数」

### 7.2 `rep-location` 库位

| 列名 | 中文 | 类型 | 示例 |
|---|---|---|---|
| code | 库位编码 | 文本 | `A-01-01-01` |
| wh | 所属仓库 | 枚举 | WH01 |
| zone | 库区 | 文本 | A区-板材 |
| type | 库位类型 | 枚举 | 存储位 / 货架位 / 地堆位 / 托盘位 / 收货暂存 / 发货暂存 / 不良品位 |
| cap | 设计容量 | 文本 | 20 吨 |
| used | 占用率(%) | 数值 | 86 |
| mat | 当前物料 | 文本 | M-1001 |
| status | 状态 | 空闲 / 占用 / 冻结 | 占用 |
| abc | ABC 分类 | A / B / C | A |

**库位编码规则**：`库区-巷道-货架-层位`，各段 2 位数字。这条规则要写在页面副标题上，
因为客户第一个问题一定是「库位编码怎么定」。

准备 13 行，覆盖 4 个仓库，其中要有 1 个「空闲」和 1 个「冻结」。

### 7.3 `rep-partner` 往来单位

| 列名 | 中文 | 类型 |
|---|---|---|
| code | 编码 | `SUP001` / `CUS001` |
| name | 单位名称 | 文本 |
| kind | 类型 | 供应商 / 客户 |
| contact | 联系人 | 文本 |
| phone | 联系电话 | 文本 |
| addr | 地址 | 文本 |
| level | 等级 | A / B / C |
| status | 合作状态 | 合作中 / 暂停 |

供应商与客户**共用一张表**，用 `kind` 区分。别做成两张表 —— 退货入库时供应商栏要填客户，两张表会让你写两套下拉逻辑。

### 7.4 `rep-receipt` 入库单（表头）

| 列名 | 中文 | 示例 |
|---|---|---|
| no | 入库单号 | `RC20260901001` |
| bizType | 业务类型 | 采购入库 / 生产入库 / 退货入库 / 调拨入库 / 盘盈入库 |
| po | 来源单号 | `PO20260828007` |
| partner | 供应商 / 来源 | 宁波宏远金属材料有限公司 |
| wh | 收货仓库 | WH01 |
| lines | 行数 | 3 |
| qtyPlan | 应收数量 | 48 |
| qtyReal | 实收数量 | 48 |
| status | 单据状态 | 待收货 / 收货中 / 待上架 / 已完成 / 已作废 |
| creator | 制单人 | 王强 |
| createAt | 制单时间 | `2026-09-01 08:42` |
| arriveAt | 到货时间 | `2026-09-01 09:10` |

**单号规则**：`RC + yyyyMMdd + 3 位流水`。出库 `DO`、移库 `TR`、盘点 `PD`、采购订单 `PO`、销售订单 `SO`、生产工单 `MO`。

准备 8 行，**5 种状态每种至少 1 行** —— 状态统计卡要能显示出 `1/1/2/3/1`。

### 7.5 `rep-receipt-line` 入库单行

| 列名 | 中文 | 说明 |
|---|---|---|
| no | 所属单号 | 关联字段，Axure 里靠它做筛选 |
| i | 行号 | 1、2、3… |
| code / name / spec / unit | 物料四要素 | 从 `rep-material` 带出 |
| qtyPlan | 应收 | |
| qtyReal | 实收 | 收货登记时录入 |
| qtyBad | 不良数量 | 质检登记时录入 |
| batch | 批次号 | 留空则按 `B + yyMMdd + 流水` 自动生成 |
| prodDate | 生产日期 | |
| loc | 上架库位 | 上架时录入，未上架为空 |
| qc | 质检结论 | 合格 / 待检 / 部分让步 / 不合格 |

> Axure 里主子表怎么做：子表中继器**不要**分单独页面。在抽屉打开时，
> 对子表中继器执行「添加筛选」，条件 `[[Item.no == LVAR_no]]`，`LVAR_no` 是点击行时存的局部变量。
> 关抽屉时「移除筛选」，否则下次打开会叠加。

### 7.6 `rep-outbound` 出库单（表头）

| 列名 | 中文 | 示例 |
|---|---|---|
| no | 出库单号 | `DO20260902001` |
| outType | 出库类型 | 销售出库 / 生产领料 / 退货出库 / 调拨出库 / 盘亏出库 / 报废出库 |
| src | 来源单号 | `SO20260901012` |
| partner | 客户 / 领用部门 | |
| wh | 发货仓库 | WH02 |
| lines | 行数 | 2 |
| qtyPlan | 应发数量 | 24 |
| qtyPick | 已拣数量 | 24 |
| status | 单据状态 | 待审核 / 待拣货 / 拣货中 / 待发运 / 已完成 / 已作废 |
| priority | 优先级 | 高 / 中 / 低 |
| creator / createAt | 制单人 / 时间 | |
| needDate | 需求日期 | `2026-09-03` |

准备 8 行，6 种状态全覆盖。

### 7.7 `rep-outbound-line` 出库单行

比入库行多两个关键字段：

| 列名 | 中文 | 为什么重要 |
|---|---|---|
| loc | 建议库位 | 系统按「先进先出 + 就近」推荐，拣货员可改 |
| avail | 库位可用量 | **拣货页要拿它和 qtyPlan 比，不足则标红阻止提交** |
| status | 行状态 | 待拣货 / 部分拣货 / 已拣货 |

> 造数据时注意**表头 `qtyPlan` 必须等于所有行 `qtyPlan` 之和**。
> HTML 原型第一版就是因为表头写 5200、行加起来 5212，被一眼看出来。数值对不上，客户会怀疑整份原型。

### 7.8 `rep-stock` 库存

库存粒度 = **物料 + 批次 + 库位**，三者任一不同就是一条独立记录。

| 列名 | 中文 | 说明 |
|---|---|---|
| code / name / spec / unit | 物料四要素 | |
| wh | 仓库 | |
| loc | 库位 | |
| batch | 批次 | 不管控批次的物料填 `—` |
| inDate | 入库日期 | 算库龄用 |
| onHand | 现存量 | |
| locked | 锁定量 | 被已审核出库单占用 |
| avail | 可用量 | **`= onHand - locked`，Axure 里用公式列，不要手填** |
| age | 库龄（天） | `= 统计日 - inDate` |
| amount | 库存金额 | `= onHand × price` |

准备 13 行。**`onHand`、`locked`、`avail` 三者必须自洽**，且和 `rep-material` 的汇总数一致。

### 7.9 `rep-ledger` 库存流水

只增不改的台账，用来跟财务对账、追溯批次去向。

| 列名 | 中文 | 说明 |
|---|---|---|
| time | 发生时间 | |
| no | 来源单号 | 点击可回跳原单据 |
| biz | 业务类型 | 采购入库 / 生产领料 / 库内移库 / 盘亏出库 … |
| dir | 方向 | 入 / 出 / 转 |
| code / name / batch | 物料与批次 | |
| wh / loc | 仓库 / 库位 | |
| before | 变动前 | |
| qty | 变动量 | **带符号**：入为正、出为负 |
| after | 变动后 | |
| op | 操作人 | |

**造数据的硬要求**：`before + qty = after`，且同一物料按时间排序后**首尾能接上**。
这张表客户看得最细 —— 财务出身的评审人一定会拿计算器加一遍。

### 7.10 其余数据集（字段较少，列要点）

| 中继器 | 关键列 |
|---|---|
| `rep-transfer` 移库单 | no / kind(库内移库·跨仓调拨) / fromWh / toWh / lines / qty / reason / status(待执行·执行中·已完成·已作废) / creator / createAt |
| `rep-stocktake` 盘点单 | no / kind(全盘·抽盘·循环盘点·动碰盘点) / wh / scope / planLines / doneLines / diffLines / status(盘点中·待审核·已完成) / owner / planDate / finishDate |
| `rep-stocktake-line` 盘点行 | no / i / code / name / spec / unit / loc / batch / sysQty(账面) / realQty(实盘) / diff / result(一致·盘盈·盘亏·待盘) |
| `rep-trend` 趋势 | month / inAmt / outAmt（12 行，近 12 个月，单位万元） |
| `rep-age-dist` 库龄分布 | bucket(0-30·31-60·61-90·91-180·180天以上) / amount / skuCount |
| `rep-slow-move` 呆滞料 | code / name / spec / unit / onHand / amount / lastOut / days / suggest(折价处理·调拨他仓·继续观察·物料停用) |
| `rep-summary-cat` 分类汇总 | cat / inAmt / outAmt / endAmt / turnover |
| `rep-user` 用户 | account / name / dept / role / whScope / phone / status / lastLogin |
| `rep-role` 角色 | code / name / users / desc |
| `rep-perm` 权限树 | m(菜单模块) / items(操作项，逗号分隔) |
| `rep-todo` 待办 | ico / title / sub / count / route |
| `rep-alert` 异常提醒 | level(critical·warning·serious) / title / time / act |
| `rep-log` 操作日志 | time / user / ip / module / act / target / result |

---

## 8. 单据状态机与业务规则

原型的价值 80% 在这一章。界面好看客户点点头，规则说清楚客户才会签字。

### 8.1 入库单状态机

```
        新建
         │ 提交
         ↓
      ┌─────┐  收货登记（部分）  ┌─────┐
      │待收货├──────────────────→│收货中│
      └──┬──┘                    └──┬──┘
         │ 收货登记（全部）          │ 收货完成
         └────────────┬─────────────┘
                      ↓
                  ┌─────┐  上架确认   ┌─────┐
                  │待上架├───────────→│已完成│
                  └─────┘             └─────┘
    任意非终态 ──作废──→ 已作废（终态，不可恢复）
```

规则：

1. **实收 ≠ 应收 允许**，但差异 > ±5% 时弹二次确认，要求填差异原因。
2. `qtyBad > 0` 时，不良品自动分流到 **WH04 不良品仓**，生成一条独立上架行。
3. `qc = 不合格` 的行**不允许上架**，只能退货或让步接收。
4. 上架确认是**库存产生的唯一时点** —— 收货只改单据，不改库存。这条要在原型里用文字标出来。
5. 已完成 / 已作废是终态，不提供任何编辑入口（连「查看」页里的按钮都要隐藏，不是禁用）。

### 8.2 出库单状态机

```
   新建 ──提交──→ 待审核 ──审核通过──→ 待拣货
                     │                   │ 拣货（部分）
                     │驳回               ↓
                     ↓              ┌───────┐
                  退回制单人         │拣货中 │
                                    └───┬───┘
                                        │ 拣货完成
                                        ↓
                                    ┌──────┐ 复核发运 ┌─────┐
                                    │待发运├─────────→│已完成│
                                    └──────┘          └─────┘
   任意非终态 ──作废──→ 已作废（同时释放已锁定库存）
```

规则：

1. **审核通过即锁定库存**：`locked += qtyPlan`，`avail` 随之下降。这是「可用量」这个字段存在的唯一理由。
2. 保存出库单时校验可用量不足 → 标红该行，提示 `可用 X，缺口 Y`，给两个选项：「按可用量下推」或「保留缺口待补货」。
3. 拣货提交三条校验（原型里在拣货页底部用蓝色说明框写出来）：
   - 已拣 > 应拣 → **拦截**，「超拣不允许」
   - 已拣 < 应拣 → 二次确认，可选「部分发货」或「等待补货」
   - 某行 `avail < qtyPlan` → 该行标红并**阻止整单提交**，需先「改库位」或「标记缺货」
4. **确认发运才真正扣库存**：`onHand -= qty`、`locked -= qty`、写出库流水、回写来源销售订单的已发货数量。
5. 作废时必须释放锁定库存，否则库存会「凭空消失」—— 这是 WMS 最常见的线上事故，原型里要在作废弹窗写明。

### 8.3 批次与先进先出（FIFO）

- 出库单默认「系统分配（先进先出）」，按 `inDate` 升序取批次。
- 允许「手工指定」，但要留操作日志（谁在什么时候绕过了 FIFO）。
- 拣货页提供「换批」按钮：换批后该行 `batch` 和 `loc` 一起变，因为批次绑在库位上。

### 8.4 盘点流程

```
创建盘点单 → 冻结范围内库位 → 录入实盘数 → 差异复盘 → 差异审核 → 过账 → 已完成
```

规则：

1. 生成盘点单即**冻结范围内库位**：期间不可拣货、不可上架，直到审核完成或作废。
2. 默认**盲盘**（录入前不显示账面数），可切明盘。盲盘更能反映真实差异，原型里默认选盲盘。
3. 差异行必须填「差异原因」（账实不符 / 破损未报 / 错发错收 / 计量误差）才能提交审核。
4. 审核过账：盘盈生成「盘盈入库」流水、盘亏生成「盘亏出库」流水，**单据号沿用盘点单号**，差异金额推财务做存货损溢。
5. 过账不可撤销。要改只能再开一张反向盘点单。

### 8.5 移库与调拨

| | 库内移库 | 跨仓调拨 |
|---|---|---|
| 库存总量 | 不变 | 不变（但分仓库存变化） |
| 流水条数 | 1 条（方向「转」） | 2 条（一出一入） |
| 是否需审核 | 否 | 是 |

### 8.6 删除 vs 停用

**基础数据一律不给真删除。** 物料/库位/往来单位只要产生过库存或流水，删除按钮点了就提示
「该物料存在库存，已阻止删除；建议改为停用」。原型里要**真的做出这个拦截提示** ——
这是最能体现「这份原型是懂业务的人做的」的细节之一。

---

## 9. 关键交互用例表

格式：**编号 · 元件 · 事件 · 条件 · 动作**。Axure 里就照这个表写用例，一行对一条。

### 9.1 全局 / 母版

| 编号 | 元件 | 事件 | 条件 | 动作 |
|---|---|---|---|---|
| UC-5.1 | 顶栏 `☰` | 单击 | — | 切换全局变量 `navCollapsed`；导航母版宽 216↔56；子项与文字隐藏；内容区左移 |
| UC-5.2 | 仓库切换器 | 单击 | — | 显示「切换仓库」弹窗（5 项：全部 + 4 个仓库） |
| UC-5.3 | 弹窗中某仓库 | 单击 | — | 设置 `curWh`；切换器文字改为所选；关闭弹窗；Toast「已切换到 XX」；**当前页所有中继器按 `wh` 重新筛选** |
| UC-5.4 | 铃铛 | 单击 | — | 打开「消息通知」抽屉，加载 `rep-alert` |
| UC-5.5 | 用户区 | 单击 | — | 打开用户信息弹窗（含「退出登录」→ 跳 login） |
| UC-5.6 | 导航分组行 | 单击 | 已展开 | 收起子项面板，箭头旋转回 `▶` |
| UC-5.7 | 导航分组行 | 单击 | 未展开 | 展开子项面板，箭头旋转为 `▼` |
| UC-5.8 | 导航子项 | 单击 | — | 设为选中（选项组 `nav-group`）；跳转对应页面 |
| UC-9.1 | 用户弹窗「切换角色」 | 单击 | — （可选增强）| 设置 `curRole`；按 `rep-perm` 重新生成导航可见项 |

### 9.2 列表页通用（17 个页面共用同一套，做一次复制）

| 编号 | 元件 | 事件 | 动作 |
|---|---|---|---|
| UC-L1 | 「查询」 | 单击 | 对中继器「添加筛选」，逐条件拼 `[[Item.xxx.indexOf(LVAR)>-1]]`；重置到第 1 页 |
| UC-L2 | 「重置」 | 单击 | 清空所有条件元件文字；中继器「移除筛选」 |
| UC-L3 | 表头全选框 | 选中 / 取消 | 遍历中继器设置每行 `chk` 列；底部显示「已选 N 条」条 |
| UC-L4 | 页码按钮 | 单击 | 中继器「设置当前页」为 `[[This.text]]` |
| UC-L5 | 「每页 N 条」 | 选项改变 | 中继器「设置每页项目数量」 |
| UC-L6 | 表格行 | 鼠标移入 / 移出 | 行底色 ↔ `#EAF2FD` |
| UC-L7 | 「导出」 | 单击 | Toast「已导出 Excel」（原型不实做） |
| UC-L8 | 列表为空 | 中继器载入时 | 若 `[[Repeater.dataCount==0]]` 显示空状态面板 |

### 9.3 入库管理

| 编号 | 元件 | 事件 | 条件 | 动作 |
|---|---|---|---|---|
| UC-IN1 | 「＋新建入库单」 | 单击 | — | 打开「新建入库单」弹窗（720 宽，表头 6 字段 + 明细行中继器） |
| UC-IN2 | 弹窗「来源单号」 | 失去焦点 | 有值 | 自动带出 `partner`，并向明细中继器添加 2 行（模拟从采购订单拉取） |
| UC-IN3 | 弹窗「保存并提交」 | 单击 | — | 关闭弹窗；Toast「入库单已创建，单号 RC20260902005」；列表中继器添加一行 |
| UC-IN4 | 单号（表格内） | 单击 | — | 存 `LVAR_no`；打开详情抽屉；子表中继器按 `no` 筛选；步骤条定位到当前状态 |
| UC-IN5 | 「收货登记」 | 单击 | `status ∈ {待收货,收货中}` | 打开收货弹窗（900 宽），实收/不良/批次/生产日期/质检结论可编辑 |
| UC-IN6 | 收货弹窗「提交收货」 | 单击 | 全部行实收 = 应收 | 单据 → `待上架`；Toast |
| UC-IN7 | 收货弹窗「提交收货」 | 单击 | 有行实收 < 应收 | 弹二次确认「存在收货差异，是否继续」；确认后 → `收货中` |
| UC-IN8 | 收货弹窗「提交收货」 | 单击 | 差异 > ±5% | **拦截**，要求填写差异原因后方可提交 |
| UC-IN9 | 「上架」 | 单击 | `status = 待上架` | 打开上架弹窗，库位下拉**排除 `status=冻结` 的库位**，默认选推荐库位 |
| UC-IN10 | 上架弹窗「确认上架」 | 单击 | — | 单据 → `已完成`；Toast「库存与流水已更新」；`rep-stock` 加行、`rep-ledger` 加行 |
| UC-IN11 | 「作废」 | 单击 | 非终态 | 弹作废弹窗，必填作废原因；确认后 → `已作废` |
| UC-IN12 | 作业台「扫描单号」 | 文字改变 | 匹配到待收货单 | 直接打开收货弹窗 |
| UC-IN13 | 作业台「扫描单号」 | 文字改变 | 匹配到终态单 | Toast（警告色）「该单据已结束，不可操作」 |

**推荐库位算法**（写在上架弹窗的说明条里）：同物料优先 → 同库区就近 → 剩余容量最大。

### 9.4 出库管理

| 编号 | 元件 | 事件 | 条件 | 动作 |
|---|---|---|---|---|
| UC-OUT1 | 「＋新建出库单」 | 单击 | — | 打开新建弹窗；选物料后自动带出「当前可用」 |
| UC-OUT2 | 新建弹窗「保存并提交」 | 单击 | 有行 `qty > avail` | 该行标红，提示 `可用 X，缺口 Y`，给「按可用量下推 / 保留缺口」二选一 |
| UC-OUT3 | 「审核」 | 单击 | `status = 待审核` | 打开审核弹窗（显示库存校验、信用校验结果） |
| UC-OUT4 | 审核弹窗「审核通过」 | 单击 | — | → `待拣货`；`locked += qtyPlan`；Toast「库存已锁定」 |
| UC-OUT5 | 审核弹窗「驳回」 | 单击 | — | → 退回制单人；Toast（警告色） |
| UC-OUT6 | 「拣货」 | 单击 | `status ∈ {待拣货,拣货中}` | **跳转拣货作业页**（带单号参数），不是弹窗 —— 拣货是长时作业，需要整页 |
| UC-OUT7 | 拣货页「确认拣入」 | 单击 | — | 对应行 `qtyPick += 本次数量`；行进度条与行状态刷新 |
| UC-OUT8 | 拣货页「提交拣货」 | 单击 | 有行 `avail < qtyPlan` | **拦截**，标红该行 |
| UC-OUT9 | 拣货页「提交拣货」 | 单击 | 有行未拣满 | 弹「部分提交 / 整单完成（未拣记缺货）」二选一 |
| UC-OUT10 | 拣货页「提交拣货」 | 单击 | 全部拣满 | → `待发运` |
| UC-OUT11 | 「复核发运」 | 单击 | `status = 待发运` | 打开发运弹窗（复核人 / 发运方式 / 承运商 / 件数 / 重量 / 发运时间） |
| UC-OUT12 | 发运弹窗「确认发运」 | 单击 | — | → `已完成`；`onHand -= qty`、`locked -= qty`；写出库流水；回写来源单 |
| UC-OUT13 | 「作废」 | 单击 | 非终态 | 弹窗必填原因；确认后 → `已作废`，**并释放 `locked`** |
| UC-OUT14 | 拣货页「换批」 | 单击 | — | 该行 `batch` 与 `loc` 同时改为最近入库批次；Toast |
| UC-OUT15 | 拣货页「路径优化」 | 单击 | — | 明细中继器按 `loc` 升序重排；Toast「已按库位路径重新排序」 |

### 9.5 库存与库内作业

| 编号 | 元件 | 事件 | 动作 |
|---|---|---|---|
| UC-STK1 | 库存表「流水」 | 单击 | 打开物料详情抽屉：概览 + 分库位分布 + 库存流水三段 |
| UC-STK2 | 「冻结库存」 | 单击 | 打开冻结弹窗（维度：按库位 / 按物料+批次 / 按整仓；必填原因） |
| UC-STK3 | 冻结弹窗确认 | 单击 | 该范围 `avail` 不再计入；Toast。说明条注明「已锁定的出库单需先释放」 |
| UC-STK4 | 「按物料汇总」 | 单击 | 中继器切分组：按 `code` 聚合，`onHand/locked/avail/amount` 求和 |
| UC-STK5 | 流水页「来源单号」 | 单击 | 回跳对应单据详情抽屉 |
| UC-WIP1 | 「＋新建移库单」 | 单击 | 打开弹窗；明细从库存查询选入，目标库位下拉排除冻结库位 |
| UC-WIP2 | 移库单「执行」 | 单击 | `待执行 → 执行中` |
| UC-WIP3 | 移库单「完成」 | 单击 | `执行中 → 已完成`；写流水（库内 1 条方向「转」，跨仓 2 条） |
| UC-WIP4 | 「＋新建盘点单」 | 单击 | 打开弹窗（方式/仓库/范围/分类/日期/负责人/盲盘明盘） |
| UC-WIP5 | 盘点弹窗「生成盘点单」 | 单击 | Toast「范围内 18 个库位已冻结」 |
| UC-WIP6 | 盘点单号 | 单击 | `status=盘点中` → 打开录入抽屉（实盘数可编辑）；否则只读 |
| UC-WIP7 | 实盘数输入框 | 文字改变 | 实时算 `diff = realQty - sysQty`，刷新「结论」标签（一致/盘盈/盘亏） |
| UC-WIP8 | 「切换明盘」 | 单击 | 显示/隐藏「账面数」列 |
| UC-WIP9 | 「差异审核」 | 单击 | 打开审核弹窗，仅列 `diff ≠ 0` 的行，每行必选差异原因 |
| UC-WIP10 | 审核弹窗「审核过账」 | 单击 | → `已完成`；生成盘盈/盘亏流水；Toast「库存已调整」 |

### 9.6 报表与系统管理

| 编号 | 元件 | 事件 | 动作 |
|---|---|---|---|
| UC-RPT1 | 「图表 / 表格」切换 | 单击 | 切换动态面板状态。**这个必须做** —— 图表的无障碍兜底，也是客户核对数字的唯一途径 |
| UC-RPT2 | 折线图 | 鼠标移动 | 显示竖向准星线 + 两个序列的圆点 + Tooltip（月份 / 入库 / 出库） |
| UC-RPT3 | 柱图柱子 | 鼠标移入 | 显示 Tooltip（区间 / 金额 / 涉及物料数） |
| UC-RPT4 | 「推送处理建议」 | 单击 | Toast「已推送 4 项呆滞料处理建议给采购与计划」 |
| UC-SYS1 | 「配置权限」 | 单击 | 打开权限弹窗：8 个菜单分组 × 操作项复选框 + 数据权限两个下拉 |
| UC-SYS2 | 分组级复选框 | 选中 / 取消 | 联动其下所有操作项；子项部分勾选时父项显示半选态 |
| UC-SYS3 | 「重置密码」 | 单击 | Toast「已重置密码，初始密码已短信发送」 |
| UC-SYS4 | 「删除角色」 | 单击 | 若 `users > 0` → Toast（警告色）「该角色已关联用户，不可删除」 |

---

## 10. 动态面板清单

Axure 里所有「浮层 / 多态」都是动态面板。统一规格，避免每个页面尺寸不一。

| 面板名 | 尺寸 | 层级 | 用途 |
|---|---|---|---|
| `dp-遮罩` | 1440 × 900 | 固定到浏览器 | 半透明黑 `rgba(22,32,46,.45)`，点击关闭 |
| `dp-弹窗-小` | 520 × 自适应 | 居中 | 确认类、作废类、简单表单 |
| `dp-弹窗-中` | 720 × 自适应 | 居中 | 物料表单、上架确认、权限配置 |
| `dp-弹窗-大` | 900 × 自适应 | 居中 | 新建单据、收货登记（含明细表格） |
| `dp-抽屉` | 680 × 900 | 固定右侧 | 单据详情、物料详情、消息通知 |
| `dp-Toast` | 自适应 × 34 | 固定顶部居中 | 深色胶囊，2.2 秒后自动隐藏 |
| `dp-导航子项` × 8 | 216 × 自适应 | 内联 | 导航分组展开/收起 |
| `dp-图表容器` × 3 | 自适应 × 260 | 内联 | 2 个状态：`图表` / `表格` |
| `dp-选项卡内容` | 自适应 | 内联 | 详情抽屉内的多标签切换 |

**弹窗和抽屉都要配遮罩**，且遮罩要先显示、面板后显示（否则遮罩会盖住面板）。
Toast 用「显示 → 等待 2200ms → 隐藏」三步动作，不要用循环。

---

## 11. 样式规范

对应 `prototype/assets/style.css` 顶部的 `:root` 变量。在 Axure「样式管理器」里建同名样式。

### 11.1 颜色 — 面与墨

| 角色 | 色值 | 用在哪 |
|---|---|---|
| 主色 | `#2A78D6` | 主按钮、链接、选中态 |
| 主色-悬停 | `#256ABF` | |
| 主色-按下 | `#1C5CAB` | |
| 主色-浅底 | `#EAF2FD` | 表格行悬停、说明框底色 |
| 页面底 | `#F4F6F9` | 内容区背景 |
| 卡片底 | `#FFFFFF` | |
| 表头底 | `#FAFBFC` | |
| 导航底 | `#16202E` | |
| 导航悬停 | `#1E2A3A` | |
| 主文字 | `#16202E` | |
| 次文字 | `#5A6474` | 字段标签、副标题 |
| 弱文字 | `#8A93A3` | 面包屑、占位符、坐标轴 |
| 分割线 | `#E5E8EE` | 表格行线、卡片描边 |
| 强分割线 | `#D3D8E2` | 输入框描边 |

### 11.2 颜色 — 状态（语义固定，永不复用）

| 语义 | 色值 | 标签底 / 字 |
|---|---|---|
| 成功 good | `#0CA30C` | `#EAF7EA` / `#0A7C0A` |
| 警告 warning | `#FAB219` | `#FFF6E0` / `#8A5E00` |
| 中度 serious | `#EC835A` | `#FDEEE6` / `#A4522C` |
| 危险 critical | `#D03B3B` | `#FDEEEE` / `#A72A2A` |
| 信息 info | `#2A78D6` | `#EAF2FD` / `#1C5CAB` |

**状态标签一律「图标 + 文字」，绝不只靠颜色。** 一是 4% 的男性有色觉障碍，
二是 WMS 经常被打印成黑白拣货单。图标映射：

| 状态 | 图标 | 配色 |
|---|---|---|
| 待收货 / 待审核 / 待拣货 / 待执行 | `○` | info |
| 收货中 / 拣货中 / 执行中 / 盘点中 | `◐` | warning |
| 待上架 | `⇧` | warning |
| 待发运 | `➜` | serious |
| 已完成 / 合格 / 一致 / 启用 | `✓` | good |
| 已作废 / 停用 | `⊘` | 灰（无色） |
| 冻结 / 不合格 | `✕` | critical |
| 盘亏 | `▼` | critical |
| 盘盈 | `▲` | warning |
| 优先级 高 / 中 / 低 | `▲` / `■` / `▼` | critical / warning / 灰 |

### 11.3 进度条三档

| 占用率 / 进度 | 填充色 | 轨道色 |
|---|---|---|
| < 70% | `#2A78D6` | `#CDE2FB` |
| 70 ~ 84% | `#FAB219` | `#FDF0D5` |
| ≥ 85% | `#D03B3B` | `#F7DADA` |

轨道用**同色系浅色**，不用灰色 —— 这样整条进度条都在传达严重程度。

### 11.4 图表专用

只用 2 个序列色，且**已通过色觉障碍模拟校验**（白底，protan ΔE 24.7 / tritan 32.7 / 正常视觉 ΔE 33.6，全项通过）：

| 序列 | 色值 | 用途 |
|---|---|---|
| 序列 1 | `#2A78D6` | 入库 |
| 序列 2 | `#EB6834` | 出库 |
| 网格线 | `#E1E0D9` | 1px 实线，不要虚线 |
| 坐标轴 | `#C3C2B7` | |

图表六条硬规则：

1. **绝不做双 Y 轴。** 两个量纲不同就拆两张图或换算成同一口径（本原型统一折算成「万元」）。
2. 折线 **2px**，圆头圆角；端点圆点 **≥ 8px 直径**，带 2px 白色描边（跨线时才看得清）。
3. 柱子**最宽 24px**，柱顶 4px 圆角、底部方角；柱宽不要填满槽位，留白是设计的一部分。
4. **≥ 2 个序列必须有图例**；单序列不要图例（标题已经说了画的是什么）。
5. **不要每个点都标数值。** 单序列柱图标在柱顶；多序列折线端点会重叠，改用「图例 + Tooltip + 表格视图」。
6. **文字永不使用序列色**，一律用文字色，靠旁边的色块传达身份。

Y 轴刻度取整：选「能用 ≤5 段覆盖最大值的最小整齐步长」。
比如最大值 402 → 步长 100、顶格 500（**不是**步长 200 顶格 800 —— 那样数据只占半个画布高）。

### 11.5 字体与字号

全站 `Microsoft YaHei`（Mac 上 `PingFang SC`），**不要用任何衬线体或装饰字体**。

| 用途 | 字号 | 字重 |
|---|---|---|
| 页标题 | 19 | 600 |
| 卡片标题 | 14 | 600 |
| 正文 / 表格 | 13 | 400 |
| 字段标签 / 副标题 | 12 | 400 |
| 表格内二行小字 | 11 | 400 |
| 统计卡数值 | 26 | 600 |

**数字对齐**：表格里的数字列用**等宽数字**（`tabular-nums`）并右对齐；
统计卡的大号数字用**比例数字**（等宽会让 `121` 这种数看起来松散）。

### 11.6 尺寸与间距

| 项 | 值 |
|---|---|
| 顶栏高 | 56 |
| 导航宽 / 折叠后 | 216 / 56 |
| 导航分组行高 / 子项行高 | 40 / 34 |
| 按钮高（常规 / 小） | 30 / 26 |
| 输入框高 | 30 |
| 表格行高（单行 / 双行） | 38 / 55 |
| 表格单元内边距 | 9 × 12 |
| 卡片圆角 / 按钮圆角 | 10 / 6 |
| 卡片间距 | 14 |
| 内容区左右内边距 | 20 |

投影只用两级：卡片 `0 1px 2px rgba(22,32,46,.06)`，浮层 `0 12px 40px rgba(22,32,46,.18)`。

---

## 12. 页面级线框

只画 5 个有代表性的。其余页面照「查询条件卡 + 表格卡」的模板套。

### 12.1 登录页

```
┌──────────────────────────────────┬─────────────────────┐
│                                  │                     │
│  ▪ 智云仓 WMS                     │   账号登录           │
│                                  │   请使用企业账号登录  │
│  仓库管理系统                     │                     │
│  覆盖入库收货、上架、拣货发运、    │   登录账号           │
│  库位管理、盘点与库龄分析的        │   [lina___________] │
│  一体化仓储作业平台。              │   登录密码           │
│                                  │   [••••••_________] │
│  （深蓝渐变 + 淡网格纹理）         │   默认仓库           │
│                                  │   [全部仓库      ▼] │
│                                  │   ☑记住账号  忘记密码?│
│  4        132      853.4万  2.6次 │   [    登 录    ]   │
│  在管仓库  库位     在库金额 年周转│   ┌───────────────┐ │
│                                  │   │原型演示账号…   │ │
└──────────────────────────────────┴───┴───────────────┴─┘
        1020 宽                            420 宽
```

### 12.2 工作台

```
┌─顶栏──────────────────────────────────────────────────────┐
├─导航─┬──────────────────────────────────────────────────┤
│工作台│ 工作台                                            │
│ 仓库 │ 仓库总览                        [导出][＋新建入库单]│
│ 总览 │ 数据截至 2026-09-02 14:00 · 当前仓库：全部（4 个） │
│基础数│ ┌────────┬────────┬────────┬────────┐            │
│据    │ │库存总金额│今日入库单│今日出库单│库存预警物料│      │
│入库管│ │853.4万元│  4 张  │  6 张  │  4 项  │            │
│理    │ │▲2.8%环比│—1张待收货│—2张待审核│▼较昨日+1│         │
│出库管│ └────────┴────────┴────────┴────────┘            │
│理    │ ┌──────────────────────┬──────────────────────┐  │
│库存管│ │出入库金额趋势  ─入库 ─出库│待办事项  按角色权限过滤│  │
│理    │ │             [图表][表格]│ ↓ 待收货入库单     ①  │  │
│库内作│ │ 500┤                   │ ⇧ 待上架          ②  │  │
│业    │ │ 400┤    ╱╲   ╱─╲       │ ✓ 出库单待审核     ②  │  │
│报表分│ │ 200┤ ╱─╯  ╲─╯    ╲     │ ↑ 待拣货          ③  │  │
│析    │ │   0└─────────────────  │ ≡ 盘点差异待审核   ①  │  │
│系统管│ │    25-10        26-09  │ ! 低于安全库存     ④  │  │
│理    │ └──────────────────────┴──────────────────────┘  │
│      │ ┌──────────────────────┬──────────────────────┐  │
│      │ │库存预警 低于安全下限   │异常提醒  最近 24 小时  │  │
│      │ │编码 名称 现存 下限 缺口│! M-3002 可用 5 台…    │  │
│      │ │M-1002 螺栓 3200 5000 │! M-1002 可用 2600 个… │  │
│      │ └──────────────────────┴──────────────────────┘  │
│      │ ┌─库位占用率 Top ──────────────────────────────┐ │
│      │ │库位编码 仓库 库区 类型 容量 占用率            │ │
│      │ │B-01-01-01 WH02 B区 货架 60台 ▇▇▇▇▇░ 92%     │ │
│      │ └────────────────────────────────────────────┘ │
└──────┴──────────────────────────────────────────────────┘
```

### 12.3 入库单管理（列表页模板）

```
入库管理 / 入库单管理
入库单管理                                    [打印] [＋新建入库单]
单据流程：新建 → 收货登记 → 质检 → 上架 → 完成
┌──────┬──────┬──────┬──────┬──────┐
│待收货 │收货中 │待上架 │已完成 │已作废 │   ← 5 个状态统计卡，点击=按状态筛选
│ 1 张 │ 1 张 │ 2 张 │ 3 张 │ 1 张 │
└──────┴──────┴──────┴──────┴──────┘
┌─查询条件卡─────────────────────────────────────────────┐
│入库单号  业务类型  供应商/来源  收货仓库  单据状态       │
│[RC…  ]  [全部 ▼] [        ]  [全部 ▼] [全部 ▼]        │
│制单开始日期      制单结束日期                [查询][重置]│
│[2026/08/26]     [2026/09/02]                          │
└──────────────────────────────────────────────────────┘
┌─入库单列表──────────────────────────────────[导出]────┐
│☐│入库单号      │业务类型│来源单号  │供应商 │仓库│行│应收│实收│状态  │制单人│操作      │
│☐│RC20260901001│采购入库│PO…828007│宁波宏远│WH01│3│ 48│ 48│✓已完成│王强 │查看      │
│☐│RC20260901002│采购入库│PO…829013│苏州精工│WH01│2│20000│20000│⇧待上架│王强│查看 上架 作废│
│☐│RC20260902001│采购入库│PO…830002│上海恒信│WH01│2│ 60│  0│○待收货│王强│查看 收货登记 作废│
│☐│RC20260902002│生产入库│MO…901005│装配一车间│WH02│1│30│18│◐收货中│李娜│查看 收货登记 作废│
├──────────────────────────────────────────────────────┤
│共 8 条 · 每页 [20▼] 条                    ‹ [1] ›     │
└──────────────────────────────────────────────────────┘
```

**注意操作列按状态变化** —— 这是列表页最容易做错的地方。不要所有行都摆一样的按钮。

### 12.4 入库单详情（抽屉，680 宽）

```
                        ┌─────────────────────────────────┐
                        │ RC20260901002  ⇧待上架       ✕ │
                        ├─────────────────────────────────┤
                        │┌─────────────────────────────┐  │
                        ││ ✓─── ✓─── ③─── ④─── ⑤       │  │
                        ││新建 收货 质检 上架 完成      │  │
                        │└─────────────────────────────┘  │
                        │┌─单据信息────────────────────┐  │
                        ││业务类型 采购入库  来源 PO…    │  │
                        ││供应商 苏州精工…  仓库 WH01   │  │
                        ││制单人 王强  制单 09-01 10:05 │  │
                        ││到货 09-01 10:30 随货 送货单  │  │
                        │└─────────────────────────────┘  │
                        │┌─入库明细  2 行───────────────┐ │
                        ││#│物料 │单位│应收 │实收│不良│批次│库位│质检│
                        ││1│M-1002│个│18000│18000│ 0│B…│A-02│✓合格│
                        ││2│M-1003│个│ 2000│ 1988│12│B…│A-02│△让步│
                        ││ │合计  │  │20000│19988│12│  │   │   │
                        │└─────────────────────────────┘ │
                        │┌─操作记录────────────────────┐  │
                        ││时间 操作人 动作 备注         │  │
                        │└─────────────────────────────┘  │
                        ├─────────────────────────────────┤
                        │        [关闭][打印单据][上架确认]│
                        └─────────────────────────────────┘
```

### 12.5 拣货作业（整页，非弹窗）

```
出库管理 / 拣货作业
拣货作业 · DO20260902002              [路径优化][打印拣货单][提交拣货]
生产领料 · 装配一车间 · 需求日期 2026-09-02
┌────────┬────────┬────────┬────────┐
│明细行数 │已完成行 │应发总量 │拣货进度 │
│  4 行  │  0 行  │ 4,212  │  74 %  │
│        │—剩余4行│（混单位）│—高优先级│
└────────┴────────┴────────┴────────┘
┌─扫码拣货──────────────────────────────────────────────┐
│扫描库位码       扫描物料条码        本次拣货数量        │
│[如 A-02-03-01] [扫码后自动匹配明细行] [0]  [确认拣入][标记缺货]│
└──────────────────────────────────────────────────────┘
┌─拣货清单  按库位路径排序──────────────────────────────┐
│顺序│库位      │物料           │批次    │应拣  │已拣  │库位可用│进度  │行状态  │操作     │
│ 1 │A-02-03-01│M-1002 外六角螺栓│B2609011│3,000个│[2600]│ 3,200│▇▇░87%│◐部分拣货│换批 改库位│
│ 2 │A-02-03-02│M-1003 深沟球轴承│B2608022│  800个│[ 500]│ 1,080│▇░░63%│◐部分拣货│换批 改库位│
│ 3 │A-03-01-01│M-1004 三相电机 │B2608015│   12台│[   0]│    16│░░░ 0%│○待拣货 │换批 改库位│
│ 4 │A-02-03-01│M-1005 RVV电缆  │—      │  400米│[   0]│ 7,400│░░░ 0%│○待拣货 │换批 改库位│
└──────────────────────────────────────────────────────┘
┌─说明框（蓝底）───────────────────────────────────────┐
│拣货提交时的校验规则：                                  │
│· 已拣 > 应拣 → 拦截，「超拣不允许」                    │
│· 已拣 < 应拣 → 二次确认，可选「部分发货」或「等待补货」  │
│· 某行库位可用量不足 → 该行标红并阻止提交               │
└──────────────────────────────────────────────────────┘
```

---

## 13. 命名规范

Axure 工程里不命名，改需求时你会疯。

| 对象 | 前缀 | 示例 |
|---|---|---|
| 页面 | 模块缩写 | `in-receipt`、`out-pick`、`stk-query` |
| 母版 | `M-` | `M-顶栏`、`M-左导航` |
| 动态面板 | `dp-` | `dp-弹窗-新建入库单`、`dp-抽屉-单据详情` |
| 中继器 | `rep-` | `rep-receipt`、`rep-receipt-line` |
| 输入类元件 | `ipt-` / `sel-` / `dat-` | `ipt-单号`、`sel-业务类型`、`dat-制单开始` |
| 按钮 | `btn-` | `btn-查询`、`btn-新建入库单` |
| 全局变量 | 小驼峰 | `curWh`、`curRole`、`navCollapsed` |
| 局部变量 | `LVAR_` | `LVAR_no`、`LVAR_code` |

---

## 14. 搭建顺序与工时

**按这个顺序做。** 每一步都建立在上一步之上，跳步会返工。

| 步 | 内容 | 工时 | 完成标志 |
|---|---|---|---|
| 1 | 全局设置 + 样式管理器（第 4、11 章） | 0.5 天 | 样式面板里有全套命名样式 |
| 2 | 元件库 `WMS.rplib`（第 6 章 19 个元件） | 1 天 | 拖一个按钮出来就是最终样式 |
| 3 | 两个母版（第 5 章） | 0.5 天 | 导航三态齐全，折叠动效可用 |
| 4 | 登录页 + 工作台（第 12.1、12.2） | 1 天 | 登录能跳工作台，统计卡与待办有数据 |
| 5 | **一个列表页做到完美**：入库单管理（第 12.3） | 1 天 | 查询/分页/全选/操作列按状态变化，全通 |
| 6 | 复制第 5 步，套出其余 11 个列表页 | 1.5 天 | 只改中继器数据集和列定义 |
| 7 | 抽屉与弹窗（第 10 章，共 9 类） | 1.5 天 | 入库详情、收货、上架、审核、发运、盘点录入全部可开 |
| 8 | 拣货作业页 + 收货作业台（整页作业视图） | 1 天 | 扫码分流逻辑可演示 |
| 9 | 两个报表页（含图表 + 表格视图切换） | 1 天 | Tooltip 与准星可用，表格视图数字和图一致 |
| 10 | 系统管理 3 页 + 权限矩阵弹窗 | 0.5 天 | 分组复选框联动、半选态正确 |
| 11 | 全链路走查 + 数据自洽核对（第 15 章） | 0.5 天 | 检查清单全绿 |

**合计约 10.5 人日。** 熟手 8 天，第一次做 WMS 原型的按 14 天估。

**最省时的一条建议**：第 5 步宁可花一整天，把一个列表页的交互抠到没有瑕疵，
再复制 11 次。反过来先铺 12 个粗糙页面，最后统一改，工时会翻倍。

---

## 15. 交付检查清单

评审前逐条打勾。前 6 条是客户一定会当场发现的。

**数据自洽**

- [ ] 每张单据的表头数量 = 所有明细行数量之和
- [ ] `avail = onHand - locked`，全表无例外
- [ ] 流水表 `before + qty = after`，同物料按时间首尾相接
- [ ] 库存表按物料汇总后 = 物料表的 `onHand`
- [ ] 统计卡数字 = 表格实际行数（别写死）
- [ ] 图表「表格视图」里的数字和图形一致

**交互完整**

- [ ] 每个状态统计卡可点，点了能筛选列表
- [ ] 操作列按钮**随状态变化**，终态行没有编辑类按钮
- [ ] 所有弹窗有遮罩，点遮罩和 `✕` 都能关
- [ ] 抽屉打开时子表按单号筛选，关闭时移除筛选
- [ ] 「删除」基础数据触发「存在库存，建议停用」拦截
- [ ] 出库作废弹窗写明「释放锁定库存」
- [ ] 拣货页三条校验规则在页面上可见

**视觉规范**

- [ ] 状态标签都是「图标 + 文字」，没有纯色块
- [ ] 图表没有双 Y 轴
- [ ] 多序列图表有图例，单序列图表没有图例
- [ ] 表格数字右对齐 + 等宽数字
- [ ] 进度条按三档配色，轨道是同色系浅色
- [ ] 间距都是 8 的倍数

**演示准备**

- [ ] 有一条完整的「入库主线」可走：新建 → 收货 → 上架 → 库存变化 → 流水
- [ ] 有一条完整的「出库主线」可走：新建 → 审核 → 拣货 → 发运 → 库存变化
- [ ] 有一条「异常线」可走：库存不足 → 拦截提示 → 改库位 → 通过
- [ ] 盘点差异线可走：录入 → 差异 → 审核 → 过账

---

## 附录 A · 为什么这样设计（评审答疑）

客户/领导常问的 8 个问题，先备好答案。

| 问题 | 答案 |
|---|---|
| 为什么详情做成抽屉不做成页面？ | 独立页面返回列表会丢筛选条件和页码，仓库员工一天开几百张单，这个损耗不可接受。 |
| 为什么拣货做成整页不做成弹窗？ | 拣货是持续几十分钟的长时作业，要能扫码、改库位、换批，弹窗承载不了；且弹窗容易误关丢进度。 |
| 为什么库存粒度到「批次 + 库位」？ | 少了批次没法做 FIFO 和质量追溯；少了库位没法生成拣货路径。这是 WMS 和进销存的分界线。 |
| 为什么要「可用量」这个字段？ | 出库单审核即锁库存。没有可用量，两张单会超卖同一批货。 |
| 为什么收货不改库存，上架才改？ | 收货只代表货到了月台，还在暂存区、没质检、没库位。此时计入库存会让拣货员拣到不存在的货。 |
| 为什么基础数据不能删除？ | 删了物料，历史单据和流水就断链，财务对账直接崩。停用即可达到「不再使用」的目的。 |
| 为什么默认盲盘？ | 明盘时盘点员看到账面数会「照着抄」，差异永远是 0，盘点失去意义。 |
| 为什么图表要有表格视图？ | 一是无障碍（色觉障碍、黑白打印）；二是客户核对数字唯一靠它 —— 没人能从折线上读出「402」。 |

## 附录 B · 后续可扩展模块（本期不做，先留菜单位）

按客户提出的频率排序：

1. **PDA / 手持终端页面** —— 收货、拣货、盘点的竖屏版（375 宽），复用同一套数据集
2. **波次管理** —— 多张出库单合并拣货，电商仓必备
3. **越库直发（Cross-docking）** —— 收货后不上架直接转出库
4. **库内 3D / 平面图看货位** —— 演示效果好，实用价值中等
5. **与 ERP / 财务的接口清单** —— 单据双向同步的字段映射表
6. **KPI 看板** —— 收货及时率、拣货准确率、库位利用率、人均处理量
